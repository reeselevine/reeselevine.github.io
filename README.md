# reeselevine.github.io

Minimal React + Vite + TypeScript personal site.

## Scripts

```bash
npm install
npm run dev
npm run build
```

## Deploy

Local/manual deploy:

```bash
npm run deploy
```

GitHub Actions also builds and deploys `dist/` on pushes to `main`.
